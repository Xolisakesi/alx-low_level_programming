#include "main.h"
/**
 * main - test a fuction that prints if an integer
 * is positive or negetive, test for 0 value
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(test);
	return (0);
}
