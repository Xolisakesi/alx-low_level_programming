#include "main.h"
#include <stdio.h>
/**
 * main - prifnts program name
 * @argc: numner of arguments counter
 * @argv: argument verctor
 * Return: Always 0 sucess
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
