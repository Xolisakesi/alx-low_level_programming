#include "main.h"
/**
 * times_table - prints 9 times table
 *
 * Return: Always 0 (sucess)
 *
 **/
void times_table(void)
{
	int i, j;

	for (i = 0; i = <= 9; i++)
	{
		for (j = i * j; j <= 9; j++)
		_putchar(j);
		_putchar(',');
		_putchar(' ');
		_putchar(' ');
	}
	return (0);
}
